<table>
  <tr valign="top">
    <td>
      <img src="{{ user.avatar(50) }}" alt="{{ user.nickname }}'s Avatar'" />
    </td>
    <td>
      <i>{{ post.author.nickname }} Says:</i><br />
      {{ post.body }}
    </td>
  </tr>
</table>